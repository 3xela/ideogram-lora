# Core deps. `ideogram4` provides the model, loaders, VAE, scheduler, and the
# Qwen3-VL text encoder wiring; it pulls most of the rest transitively, but the
# important ones are pinned here for clarity.
torch>=2.11
ideogram4 @ git+https://github.com/ideogram-oss/ideogram4
transformers>=4.49.0
safetensors>=0.4.5
huggingface_hub>=0.26.0
bitsandbytes>=0.49.2   # nf4 quantized base weights
einops>=0.7.0
accelerate>=1.0.0
sentencepiece
pillow
numpy
tqdm
