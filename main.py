#!/usr/bin/env python
"""Entry point. Run `python main.py <command> [options]` (or `ilora ...` once installed).

See `python main.py --help` for the list of commands.
"""

from ideogram_lora.cli import main

if __name__ == "__main__":
    main()
